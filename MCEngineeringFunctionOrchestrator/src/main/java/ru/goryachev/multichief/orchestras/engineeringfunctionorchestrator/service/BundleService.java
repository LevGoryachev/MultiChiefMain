package ru.goryachev.multichief.orchestras.engineeringfunctionorchestrator.service;
/**
 * BundleService provides operations for composite DTOs (Bundles) using other microservices.
 * The implementations can join, separate, compose certain DTOs in case of business needs (to implement CRUD for Business-Entities).
 * @author Lev Goryachev
 * @version 1.0
 */
public interface BundleService {
    /*getBundle ();
    saveBundle();
    deleteBundle ();*/
}
